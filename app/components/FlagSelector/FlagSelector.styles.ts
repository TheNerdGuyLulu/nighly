import { StyleSheet } from 'react-native-unistyles';

export const flagSelectorStyles = StyleSheet.create(theme => ({
  icon: {
    width: '100%',
    height: '100%',
    borderRadius: 200,
    overflow: 'hidden',
  },
}));
